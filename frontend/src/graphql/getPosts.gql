query Posts(
    $tagSlug: String
    $categorySlug: String
    $pageNr: Int
) {
    posts(tagSlug: $tagSlug, categorySlug: $categorySlug, pageNr:$pageNr) {
        slug
        title
        image
        dateCreated
        likeCount
        category {
            name
            slug
        }
        owner {
            firstName
            lastName
        }
        comments {
            id
        }
    }
}